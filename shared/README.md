# Shared

This package contains TypeScript types shared across the backend and frontend.

Examples:

- telemetry payloads
- sensor reading shapes
- health/status models
- DTOs used by both API and dashboard
